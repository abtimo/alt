openapi: 3.0.1
info:
  title: 1s-mock
  description: API Definition of 1s-mock
  version: 1.0.0
servers:
- url: http://localhost:8290/1s
paths:
  /pos/{code}:
    get:
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
      responses:
        default:
          description: Default response
