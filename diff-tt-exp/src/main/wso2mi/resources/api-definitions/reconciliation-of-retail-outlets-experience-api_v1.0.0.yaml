openapi: 3.0.1
info:
  title: reconciliation-of-retail-outlets-experience-api
  description: API Definition of reconciliation-of-retail-outlets-experience-api
  version: 1.0.0
servers:
- url: http://localhost:8290/default
paths:
  /tt-check:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
          application/xml:
            schema:
              type: object
              xml:
                name: payload
        required: false
      responses:
        default:
          description: Default response
  /pos-diff-check/{code}:
    get:
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
      responses:
        default:
          description: Default response
